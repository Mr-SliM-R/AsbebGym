{
  "name": "gym-rival",
  "version": "0.1.0",
  "private": true,
  "description": "A three-user fitness rivalry app with workout tracking, challenges, ranks, and exercise animations.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "start": "npm run start -w server",
    "seed": "npm run seed -w server",
    "cf:build": "npm run build -w client",
    "cf:typecheck": "tsc -p cloudflare/tsconfig.json --noEmit",
    "cf:d1:schema": "wrangler d1 execute gym-rival-db --remote --file=cloudflare/schema.sql",
    "cf:d1:seed": "wrangler d1 execute gym-rival-db --remote --file=cloudflare/seed.sql",
    "cf:d1:users": "wrangler d1 execute gym-rival-db --remote --file=cloudflare/add-users-and-avatars.sql",
    "cf:deploy": "npm run cf:build && npm run cf:typecheck && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260426.1",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.86.0"
  }
}
